# Database — SQLite (default, zero-config dev)
DATABASE_URL="file:./dev.db"

# Database — PostgreSQL (production, requires Docker or external DB)
# DATABASE_URL="postgresql://haloyoga:haloyoga_dev@localhost:5432/haloyoga"

# NextAuth — Authentication
# Generate secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Email (optional) — Resend or Nodemailer
# RESEND_API_KEY="re_xxxxxxxx"
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER="user@example.com"
# SMTP_PASS="password"

# Site Info
NEXT_PUBLIC_SITE_NAME="הילה יוגה"
NEXT_PUBLIC_PHONE="050-1234567"
NEXT_PUBLIC_WHATSAPP="972501234567"
NEXT_PUBLIC_EMAIL="info@healing.co.il"
NEXT_PUBLIC_ADDRESS="רחוב הרופא 15, תל אביב"
